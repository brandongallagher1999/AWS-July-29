apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: nezz-takehome
  labels:
    app: nezz-takehome-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: nezz-takehome-app 